//! User configuration (`~/.config/tokscope/config.toml` via `directories`).
//!
//! TODO(scope): not read yet in the MVP. Planned keys: default agent, pricing
//! overrides, extra data dirs. Wire up with `directories::ProjectDirs` + `toml`
//! when a second consumer appears — no speculative abstraction before that.
