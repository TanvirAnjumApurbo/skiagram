//! Heuristic task-type classification (debugging vs feature work vs refactor…).
//!
//! TODO(scope): post-MVP. Classify sessions/turns from tool mix + prompt shape so
//! spend can be broken down by what the user was actually doing.
